@extends('admin_client.admin_client')

@section('content')
@php
    $clientName=$client->fullname ?? $client->full_name ?? $client->name ?? '-';
    $thaiDate=static function($value):string{if(!$value)return '-';try{$d=$value instanceof \Carbon\CarbonInterface?$value:\Carbon\Carbon::parse($value);return $d->format('d/m/').($d->year+543);}catch(\Throwable $e){return '-';}};
    $trendIcon=['up'=>'bi-arrow-up-right','same'=>'bi-arrow-right','down'=>'bi-arrow-down-right','none'=>'bi-dash'];
    $trendClass=['up'=>'up','same'=>'same','down'=>'down','none'=>'same'];
    $itemsByIndicator=$followup->items->keyBy('indicator_id');
@endphp
<style>
.idps{--bd:#e2e9f1;--txt:#203249;--muted:#6b7c90;padding-bottom:1.5rem}.idps .head,.idps .card,.idps .next{background:#fff;border:1px solid var(--bd);border-radius:16px;box-shadow:0 6px 20px rgba(30,50,75,.045)}.idps .head{padding:1rem 1.15rem;margin-bottom:1rem;display:flex;justify-content:space-between;gap:1rem;align-items:flex-start;flex-wrap:wrap}.idps h4{font-size:1.12rem;font-weight:800;margin:0;color:var(--txt)}.idps .sub{font-size:.84rem;color:var(--muted);margin-top:.28rem}.idps .actions{display:flex;gap:.5rem;flex-wrap:wrap}.idps .btnx{display:inline-flex;align-items:center;justify-content:center;gap:.4rem;min-height:40px;padding:.48rem .85rem;border-radius:10px;font-size:.8rem;font-weight:800;text-decoration:none;border:1px solid #d5e0eb;background:#fff;color:#4b6076}.idps .btnx.edit{border-color:#a9c8e8;color:#2a6dad;background:#f7fbff}.idps .btnx.delete{border-color:#f1b6bc;color:#c8424d;background:#fff8f8}.idps .card{overflow:hidden;margin-bottom:1rem}.idps .card-head{padding:.85rem 1rem;background:#fbfcfe;border-bottom:1px solid var(--bd);font-size:.92rem;font-weight:800;color:var(--txt)}.idps .card-body{padding:1rem}.idps .meta{display:grid;grid-template-columns:repeat(4,1fr);gap:.7rem}.idps .meta>div{border:1px solid #e6ebf2;border-radius:12px;padding:.7rem;background:#fafcff}.idps .label{font-size:.72rem;color:var(--muted)}.idps .value{font-size:.83rem;font-weight:800;color:#34485e;margin-top:.12rem;white-space:pre-line}.idps .domains{display:grid;grid-template-columns:repeat(4,1fr);gap:.7rem}.idps .domain{border:1px solid #e1e8f0;border-radius:13px;padding:.8rem;background:#fff}.idps .domain-name{font-size:.82rem;font-weight:800}.idps .scoreline{display:flex;align-items:center;justify-content:space-between;gap:.4rem;margin-top:.45rem}.idps .score{font-size:1.15rem;font-weight:900;color:#245f9f}.idps .trend{display:inline-flex;align-items:center;gap:.2rem;font-size:.73rem;font-weight:800;border-radius:999px;padding:.24rem .48rem}.idps .trend.up{background:#eaf8ef;color:#16764b}.idps .trend.down{background:#fff0f1;color:#b53b47}.idps .trend.same{background:#f2f4f7;color:#667383}.idps .level{font-size:.72rem;color:#687b8f;margin-top:.25rem}.idps .grid2{display:grid;grid-template-columns:1fr 1fr;gap:.75rem}.idps .box{border:1px solid #e6ebf2;border-radius:12px;padding:.75rem;background:#fcfdff}.idps .next{padding:1rem;margin-bottom:1rem;border-color:#bfd8ef;background:#f6fbff}.idps .next-title{font-size:.82rem;font-weight:900;color:#245f9f}.idps .next-text{font-size:.84rem;color:#405b75;white-space:pre-line;margin-top:.3rem}.idps .tablewrap{overflow-x:auto}.idps table{width:100%;min-width:900px;border-collapse:separate;border-spacing:0}.idps th,.idps td{padding:.65rem;border-bottom:1px solid #edf1f5;font-size:.76rem;vertical-align:top}.idps th{background:#f8fafc;color:#53657b;white-space:nowrap}.idps .change.up{color:#16764b;font-weight:800}.idps .change.down{color:#b53b47;font-weight:800}.idps .change.same{color:#687586;font-weight:800}
@media(max-width:991.98px){.idps .meta,.idps .domains{grid-template-columns:repeat(2,1fr)}.idps .grid2{grid-template-columns:1fr}}@media(max-width:575.98px){.idps .meta,.idps .domains{grid-template-columns:1fr}.idps .actions{display:grid;grid-template-columns:1fr 1fr;width:100%}.idps .actions .btnx:first-child{grid-column:1/-1}.idps .btnx{width:100%}}

/* IDP_PHASE6_SAFE_SCOPE */
.idps{width:100%;min-width:0}
.idps *{min-width:0}
.idps .value,.idps td,.idps .next-text{overflow-wrap:anywhere;word-break:break-word}
</style>
<div class="container-fluid px-2 px-lg-3 idps">
 <div class="head"><div><h4><i class="bi bi-activity me-2 text-primary"></i>ผลการติดตาม ครั้งที่ {{ $followup->followup_no }}</h4><div class="sub">ผู้รับบริการ: <strong>{{ $clientName }}</strong> • อายุ {{ $ageText }} • แผนครั้งที่ {{ $plan->plan_no }}</div></div><div class="actions"><a class="btnx" href="{{ route('individual-development.index',$client->id) }}"><i class="bi bi-arrow-left"></i>กลับหน้าหลัก</a>@if($canUpdate&&!$readOnly)<a class="btnx edit" href="{{ route('individual-development.followups.edit',[$client->id,$followup->id]) }}"><i class="bi bi-pencil-square"></i><span>แก้ไข</span></a>@endif @if($canDelete&&!$readOnly)<form method="POST" action="{{ route('individual-development.followups.destroy',[$client->id,$followup->id]) }}" class="delete-followup m-0">@csrf @method('DELETE')<button class="btnx delete" type="submit"><i class="bi bi-trash3"></i><span>ลบ</span></button></form>@endif</div></div>

 @if(session('success')||session('warning'))<div class="alert {{ session('success')?'alert-success':'alert-warning' }}">{{ session('success')??session('warning') }}</div>@endif

 <div class="card"><div class="card-head">ข้อมูลการติดตาม</div><div class="card-body"><div class="meta"><div><div class="label">วันที่ติดตาม</div><div class="value">{{ $thaiDate($followup->followup_date) }}</div></div><div><div class="label">ประเภท</div><div class="value">{{ $followupTypes[$followup->followup_type] ?? ($followup->followup_type ?: '-') }}</div></div><div><div class="label">ผู้ติดตาม</div><div class="value">{{ $followup->follower_name ?: '-' }}</div></div><div><div class="label">ผลโดยรวม</div><div class="value">{{ $resultLabels[$followup->overall_result] ?? ($followup->overall_result ?: '-') }}</div></div></div></div></div>

 <div class="card"><div class="card-head">เปรียบเทียบพัฒนาการ 4 ด้าน</div><div class="card-body"><div class="domains">@foreach($domainSummaries as $d)<div class="domain"><div class="domain-name">{{ $d['name'] }}</div><div class="scoreline"><div class="score">{{ $d['previous']!==null?number_format($d['previous'],2):'-' }} → {{ $d['current']!==null?number_format($d['current'],2):'-' }}</div><span class="trend {{ $trendClass[$d['trend']] }}"><i class="bi {{ $trendIcon[$d['trend']] }}"></i>{{ $d['delta']!==null?($d['delta']>0?'+':'').number_format($d['delta'],2):'-' }}</span></div><div class="level">{{ $d['level']?'ระดับ '.$d['level'].' • '.$d['label']:$d['label'] }}</div></div>@endforeach</div></div></div>

 <div class="card"><div class="card-head">สรุปผลการติดตาม</div><div class="card-body"><div class="grid2">@foreach(['current_situation'=>'สถานการณ์ปัจจุบัน','changes'=>'การเปลี่ยนแปลงที่พบ','positive_changes'=>'สิ่งที่ดีขึ้น','actions_taken'=>'การช่วยเหลือ/กิจกรรมที่ดำเนินการ','result'=>'ผลที่เกิดขึ้น','problem'=>'ปัญหา/อุปสรรค','client_feedback'=>'ความคิดเห็นของผู้รับบริการ','caregiver_feedback'=>'ความคิดเห็นของผู้ดูแล/ครอบครัว','suggestion'=>'ข้อเสนอแนะ'] as $field=>$label)<div class="box"><div class="label">{{ $label }}</div><div class="value">{{ $followup->{$field} ?: '-' }}</div></div>@endforeach</div></div></div>

 <div class="next"><div class="next-title"><i class="bi bi-pin-angle me-1"></i>สิ่งที่ต้องดำเนินการต่อ</div><div class="next-text">{{ $followup->next_action ?: '-' }}</div><div class="small text-muted mt-2">นัดติดตามครั้งถัดไป: {{ $thaiDate($followup->next_followup_date) }}</div></div>

 <div class="card"><div class="card-head">รายละเอียดตัวชี้วัด 20 รายการ</div><div class="tablewrap"><table><thead><tr><th>ด้าน</th><th>ตัวชี้วัด</th><th class="text-center">ครั้งก่อน</th><th class="text-center">ครั้งนี้</th><th class="text-center">แนวโน้ม</th><th>หลักฐาน/พฤติกรรม</th><th>ข้อสังเกต</th></tr></thead><tbody>@foreach($domains as $domain)@foreach($domain->indicators as $indicator)@php $item=$itemsByIndicator->get($indicator->id); $delta=($item?->score!==null&&$item?->previous_score!==null)?$item->score-$item->previous_score:null; $tc=$delta===null?'same':($delta>0?'up':($delta<0?'down':'same')); @endphp <tr><td>{{ $domain->name }}</td><td><strong>{{ $indicator->code }}</strong> {{ $indicator->name }}</td><td class="text-center">{{ $item?->previous_score ?? '-' }}</td><td class="text-center fw-bold">{{ $item?->score ?? '-' }}</td><td class="text-center"><span class="change {{ $tc }}">{{ $delta===null?'-':($delta>0?'↑ +'.$delta:($delta<0?'↓ '.$delta:'→ 0')) }}</span></td><td>{{ $item?->evidence ?: '-' }}</td><td>{{ $item?->development_note ?: '-' }}</td></tr>@endforeach @endforeach</tbody></table></div></div>
</div>
<script>document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('.delete-followup').forEach(function(f){f.addEventListener('submit',function(e){if(!window.Swal){if(!confirm('ยืนยันการลบการติดตามล่าสุด?'))e.preventDefault();return;}e.preventDefault();Swal.fire({icon:'warning',title:'ยืนยันการลบ?',text:'ลบได้เฉพาะการติดตามล่าสุด เพื่อรักษาลำดับประวัติ',showCancelButton:true,confirmButtonText:'ลบข้อมูล',cancelButtonText:'ยกเลิก'}).then(r=>{if(r.isConfirmed)f.submit();});});});});</script>
@endsection
